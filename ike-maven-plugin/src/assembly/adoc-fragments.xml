<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
          http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <!-- Publishes the reusable release-cascade doc fragments as the
         `adoc`-classified zip of ike-maven-plugin, so downstream docs
         can include:: them instead of duplicating the model
         (IKE-Network/ike-issues#708). The id becomes the artifact
         classifier (`adoc`), matching the ecosystem's doc-fragment
         convention so a consumer's `includeClassifiers=adoc` unpack
         picks it up. The fragments keep their `_cascade/` directory so
         a consumer unpacks them under src/site/asciidoc and includes
         via the same `include::_cascade/...` relative path this repo
         uses. -->
    <id>adoc</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <directory>src/site/asciidoc/_cascade</directory>
            <outputDirectory>_cascade</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>
