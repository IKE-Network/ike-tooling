= @setName@ @familyLabel@
:toc: left
:sectnums:

== What This Set Provides

// TODO: the conversational what/why/how of this set's content, with k: chips —
// e.g. k:@tag@Root[] — resolved from the extracted koncept definitions.

The @tag@ knowledge set declares its content as an append-only ledger; this guide
and its comprehensive glossary are generated from the same materialized store as
the released change set.

// The comprehensive glossary is appended automatically (koncept-glossary-all).
