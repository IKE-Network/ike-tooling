<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 https://maven.apache.org/xsd/maven-4.1.0.xsd">
    <modelVersion>4.1.0</modelVersion>

    <parent>
        <groupId>@groupId@</groupId>
        <artifactId>@artifactRoot@</artifactId>
    </parent>

    <artifactId>@slug@-changeset</artifactId>
    <packaging>jar</packaging>

    <name>@setName@ Change Set</name>
    <description>
        The released form of the @tag@ knowledge set: ike:knowledge-export replays
        the ledger and attaches the protobuf change set (classifier: changeset). A
        starter set is exactly this artifact applied to an empty base.
    </description>

    <dependencies>
        <dependency>
            <groupId>@groupId@</groupId>
            <artifactId>@slug@-terms</artifactId>
            <version>239</version>
            <scope>runtime</scope>
        </dependency>
        <!-- The knowledge-pipeline implementation, resolved by ServiceLoader inside
             the goal's forked seam. -->
        <dependency>
            <groupId>network.ike.knowledge</groupId>
            <artifactId>ike-knowledge-provider</artifactId>
            <version>${ike-knowledge-provider.version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-maven-plugin</artifactId>
                <version>@toolingVersion@</version>
                <executions>
                    <execution>
                        <id>export-knowledge-changeset</id>
                        <goals>
                            <goal>knowledge-export</goal>
                        </goals>
                        <configuration>
                            <!-- Glossary from the same loaded store — one
                                 materialization, both artifacts. -->
                            <konceptsYmlFile>${project.build.directory}/@slug@-koncepts.yml</konceptsYmlFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
