<?xml version="1.0" encoding="UTF-8"?>
<!--
  IKE Network — Knowledge SPI
  ═══════════════════════════

  The contract layer of the knowledge pipeline (IKE-Network/ike-issues#856,
  design IKE-Network/ike-issues#850): service interfaces the ike: knowledge
  goals resolve via ServiceLoader, plus the value records and properties
  codec that cross the forked-JVM seam.

  ZERO runtime dependencies by design — the IKE foundation never depends
  on the chronology store. Implementations (ike-knowledge-provider) are
  selected by an ordinary dependency at the use site, defaulted by
  ike-parent's pluginManagement.

  Build:    mvn install -pl ike-knowledge-spi
  Artifact: network.ike.tooling:ike-knowledge-spi:<version>
-->
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0
         http://maven.apache.org/xsd/maven-4.1.0.xsd">
    <modelVersion>4.1.0</modelVersion>

    <parent/>

    <artifactId>ike-knowledge-spi</artifactId>
    <packaging>jar</packaging>

    <name>IKE Knowledge SPI</name>
    <description>
        Zero-dependency contract module for the IKE knowledge pipeline:
        ServiceLoader service interfaces (assemble, export, bindings, verify),
        the ViewSpec dimension vocabulary, and the properties codec that
        crosses the forked-JVM execution seam.
    </description>
    <url>https://ike.network/ike-tooling/ike-knowledge-spi/</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>IKE Network</name>
        <url>https://ike.network</url>
    </organization>

    <distributionManagement>
        <repository>
            <id>ike-staging</id>
            <url>${deploy.staging.url}</url>
        </repository>
        <snapshotRepository>
            <id>ike-snapshots</id>
            <url>${deploy.snapshot.url}</url>
        </snapshotRepository>
        <site>
            <id>ike-site</id>
            <url>https://ike.network/ike-tooling/${project.artifactId}/</url>
        </site>
    </distributionManagement>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.deploy.skip>false</maven.deploy.skip>
        <maven.install.skip>false</maven.install.skip>
    </properties>

    <dependencies>
        <!-- Intentionally no runtime dependencies: the foundation invariant.
             Anything this module needs comes from the JDK. -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- ASM 9.9 for Java 25 classfile parsing of module-info — the same
                 override ike-parent (ike-platform) applies; ike-base-parent's
                 compiler configuration lacks it and this is the first module-info
                 in ike-tooling. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.ow2.asm</groupId>
                        <artifactId>asm</artifactId>
                        <version>9.9</version>
                    </dependency>
                    <dependency>
                        <groupId>org.ow2.asm</groupId>
                        <artifactId>asm-commons</artifactId>
                        <version>9.9</version>
                    </dependency>
                    <dependency>
                        <groupId>org.ow2.asm</groupId>
                        <artifactId>asm-tree</artifactId>
                        <version>9.9</version>
                    </dependency>
                </dependencies>
            </plugin>
            <!-- Tests live in the module's own package and exercise ServiceLoader via
                 META-INF/services — the classpath duality the forked seam runs under —
                 so they run classpath-mode, not patched onto the module path. -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
